# queryforge_ai
A sleek, hacker-style code editor where you input a slow SQL query. When you hit analyze, a glowing "AI scan" animation runs over the code, and an AI chat window slides in to rewrite the query and suggest exactly which indexes to build to reduce execution time by 94%.
